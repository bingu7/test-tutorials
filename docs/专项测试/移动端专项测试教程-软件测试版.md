---
description: 移动端专项测试教程，涵盖弱网、兼容性、性能监控、崩溃分析、权限测试等。
---
# 移动端专项测试教程（软件测试人员专用）

> 本教程面向软件测试工程师，系统讲解移动端 App 专项测试，从弱网模拟、兼容性覆盖到崩溃分析与性能监控，帮助你建立完整的移动端测试能力。

---

## 前置要求

| 项目 | 要求 | 获取方式 |
|------|------|----------|
| 软件测试基础 | 了解测试用例设计、Bug 提交流程 | 通用基础 |
| 接口测试基础 | 了解 HTTP 请求/响应、抓包工具基本使用 | [接口测试完整教程](../专项测试/接口测试完整教程-软件测试版.md) |
| 命令行基础 | 能在终端执行基本命令 | 通用基础 |
| 移动设备 | Android 真机或 iOS 真机至少一台 | 自备或团队共享 |

---

## 新手导读

移动端测试比 Web 测试多了很多"不可控因素"——不同厂商的系统、不同网络环境、不同权限状态，这些都会导致 Bug。第一遍不需要记住所有工具命令，重点理解"为什么做"和"测什么"。

建议学习顺序：

1. 理解移动端测试与 Web 测试的核心差异。
2. 用 Charles 或 Fiddler 模拟一次弱网，观察 App 表现。
3. 列出你当前项目的兼容性测试矩阵。
4. 用 adb 命令查看一次 App 的内存占用。
5. 理解崩溃日志的基本结构。
6. 实际操作一次覆盖升级测试。

能发现真实场景下的 Bug，比记住所有命令更重要。

### 版本与维护说明

| 项目 | 说明 |
|------|------|
| 适用平台 | Android 8.0+、iOS 13+ |
| 工具 | Charles、Fiddler、adb、Xcode Instruments |
| 使用建议 | 以真机为主，模拟器为辅 |
| 更新提醒 | Android 权限模型和 iOS 系统行为每版都有变化，注意核对 |

---

## 一、移动端测试概述

### 1.1 移动端测试 vs Web 测试的差异

| 维度 | Web 测试 | 移动端测试 |
|------|----------|------------|
| 设备 | 浏览器种类有限 | 机型、系统版本、分辨率碎片化严重 |
| 网络 | 通常稳定的 Wi-Fi/有线 | Wi-Fi、4G、5G、弱信号频繁切换 |
| 交互 | 鼠标点击为主 | 触摸、滑动、长按、多点触控、陀螺仪 |
| 权限 | 浏览器沙箱，权限简单 | 系统级权限（相机、定位、存储、麦克风） |
| 生命周期 | 页面切换 | 前后台切换、系统杀进程、推送唤醒 |
| 安装/更新 | 浏览器自动加载 | 安装包下载、覆盖升级、卸载重装 |

### 1.2 移动端特有的测试维度

**设备碎片化：** Android 有数千款机型、数十个厂商 ROM，同一 API 在不同设备上行为可能不同。

**网络环境：** 用户可能在电梯、地铁、山区等弱信号环境使用 App，网络随时可能中断或切换。

**系统权限：** 用户可能拒绝授权或随时撤销权限，App 必须优雅降级而非崩溃。

**系统干扰：** 来电、闹钟、低电量弹窗、系统更新提示都可能打断 App 正常流程。

### 1.3 移动端专项测试全景图

```
移动端专项测试
├── 弱网测试          ← 网络层
├── 兼容性测试        ← 设备层
├── 性能监控          ← 资源层
├── 崩溃分析          ← 稳定性层
├── 安装/升级/卸载    ← 生命周期层
├── 推送通知          ← 消息层
├── 手势与交互        ← 交互层
└── 系统权限          ← 安全层
```

---

## 二、弱网测试

### 2.1 为什么要做弱网测试

真实用户场景中，弱网无处不在：

| 场景 | 典型网络状况 |
|------|-------------|
| 电梯/地下车库 | 信号极弱或无信号 |
| 地铁/高铁 | 频繁切换基站，网络不稳定 |
| 偏远山区 | 2G/低速 3G |
| 人流密集场所 | 带宽被共享，实际速度低 |

弱网下 App 的表现直接影响用户体验：页面白屏、数据丢失、重复提交、操作无响应，都是常见问题。

### 2.2 Charles 模拟弱网

**Throttle 设置步骤：**

1. 打开 Charles → `Proxy` → `Throttle Settings`
2. 勾选 `Enable Throttling`
3. 选择预设或自定义参数：

| 预设 | 带宽 | 延迟 | 适用场景 |
|------|------|------|----------|
| 56 kbps (Modem) | 56 kbps | 300ms | 极弱网 |
| 256 kbps (DSL) | 256 kbps | 200ms | 弱 2G |
| 512 kbps | 512 kbps | 100ms | 3G |
| 2 Mbps | 2 Mbps | 50ms | 4G |

4. 可勾选 `Only for selected hosts` 限定只对特定域名限速

**自定义配置示例（模拟弱 3G）：**

```
Bandwidth: 780 kbps | Utilisation: 80%
Latency: 200ms | Reliability: 90%（10% 丢包）
```

### 2.3 Fiddler 模拟弱网

打开 Fiddler → `Rules` → `Customize Rules`，在 `OnBeforeRequest` 中添加：

```javascript
if (oSession.HostnameIs("api.yourapp.com")) {
    oSession["request-trickle-delay"] = "300";   // 发送延迟 ms/KB
    oSession["response-trickle-delay"] = "300";  // 接收延迟 ms/KB
}
```

启用：`Rules` → `Performance` → `Simulate Modem Speeds`

### 2.4 常见弱网场景速查

| 场景 | 带宽 | 延迟 | 丢包率 |
|------|------|------|--------|
| 2G (GPRS) | 50-100 kbps | 300-500ms | 2-5% |
| 弱 3G | 200-500 kbps | 150-300ms | 1-2% |
| 正常 3G | 1-2 Mbps | 100-200ms | <1% |
| 弱 4G | 2-5 Mbps | 50-100ms | <1% |
| 高延迟 | 不限 | 500ms+ | 0% |
| 断网恢复 | 0 → 恢复 | - | 100% → 0% |

### 2.5 弱网下的常见 Bug

| Bug 类型 | 表现 | 根因 |
|----------|------|------|
| **请求超时** | 页面长时间 loading 或白屏 | 未设置合理的超时时间 |
| **重复提交** | 同一订单/评论创建多条 | 前端未做防重，后端未做幂等 |
| **数据丢失** | 填写的内容提交后消失 | 缓存策略不当 |
| **图片加载失败** | 头像/商品图显示空白 | 未做降级/占位图处理 |
| **页面状态不一致** | 列表已更新但详情还是旧数据 | 缓存未刷新 |
| **断网恢复后异常** | 网络恢复后页面无法继续使用 | 未监听网络状态变化 |

---

## 三、兼容性测试

### 3.1 兼容性测试矩阵设计

核心原则：**用有限的设备覆盖最大范围的用户**。

| 维度 | Android | iOS |
|------|---------|-----|
| **OS 版本** | 8.0 / 10 / 12 / 14 / 15 | 13 / 14 / 15 / 16 / 17 |
| **屏幕尺寸** | 5.0" / 5.5" / 6.1" / 6.7" / 折叠屏 | SE / 标准 / Plus / Pro Max / iPad |
| **厂商** | 华为 / 小米 / OPPO / vivo / 三星 / 荣耀 | Apple |

**优先级策略：**

- **P0（必测）：** Top 3 机型 + 最新 OS + 最旧支持 OS
- **P1（高优）：** Top 5-10 机型 + 主流分辨率
- **P2（覆盖）：** 长尾机型 + 折叠屏 + 平板

### 3.2 真机测试 vs 云测平台

| 方式 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **自有真机** | 完全可控、调试方便 | 机型有限、成本高 | 日常自测 |
| **Testin** | 海量机型、自动化执行 | 排队等待 | 兼容性批量验证 |
| **AWS Device Farm** | 真机远程操控 | 价格较高 | 出海产品 |
| **Firebase Test Lab** | Google 官方、集成 CI | 仅原生 | Google 生态项目 |

### 3.3 兼容性测试用例模板

| 用例编号 | 测试项 | 测试步骤 | 预期结果 | 设备组合 | 实际结果 | 状态 |
|----------|--------|----------|----------|----------|----------|------|
| CP-001 | 首页加载 | 冷启动 App | 3s 内完整展示 | 小米14/Android15 | 正常 | ✅ |
| CP-002 | 首页加载 | 冷启动 App | 3s 内完整展示 | 华为Mate60/HarmonyOS4 | 正常 | ✅ |
| CP-003 | 列表滑动 | 快速滑动100条 | 无卡顿 | 三星A54/Android14 | 轻微掉帧 | ⚠️ |

### 3.4 UI 适配问题检查清单

```
□ 文字是否溢出/截断（尤其长文案、多语言）
□ 图片是否拉伸/压缩/留白
□ 按钮/输入框是否被刘海/挖孔/圆角遮挡
□ 底部导航栏是否被系统手势条遮挡
□ 横竖屏切换后布局是否正常
□ 折叠屏展开/折叠后页面是否重绘
□ 大字体/无障碍模式下是否可用
□ 深色模式下文字/图标是否可见
□ 弹窗/底部弹出在不同屏幕高度下是否完整显示
```

---

## 四、移动端性能监控

### 4.1 Android 性能监控（adb）

**内存信息：**

```bash
adb shell dumpsys meminfo com.example.app
# 关键指标：PSS Total、Java Heap、Native Heap、Graphics
```

**CPU 信息：**

```bash
adb shell dumpsys cpuinfo
adb shell top -d 1 | grep com.example.app
```

**电量消耗：**

```bash
adb shell dumpsys batterystats --reset          # 重置统计
adb shell dumpsys batterystats > battery.txt    # 导出报告
# 关注：Estimated power use、Wakelock 持有时间
```

**启动时间测量：**

```bash
adb shell am force-stop com.example.app
adb shell am start -W com.example.app/.MainActivity
# 关注：TotalTime（Activity 创建到展示）、WaitTime（含系统调度）
```

### 4.2 iOS 性能监控（Instruments）

| Instrument | 用途 | 关注指标 |
|------------|------|----------|
| Time Profiler | CPU 分析 | 主线程耗时、方法调用栈 |
| Allocations | 内存分配 | 堆内存增长、对象生命周期 |
| Leaks | 内存泄漏 | 泄漏对象数量、大小 |
| Energy Log | 电量消耗 | 能耗等级、后台活动 |
| Network | 网络流量 | 请求次数、数据量、延迟 |
| Core Animation | UI 流畅度 | FPS、掉帧数 |

启动方式：Xcode → `Product` → `Profile`（⌘I）

### 4.3 流量消耗测试

| 场景 | 关注指标 |
|------|----------|
| 首页加载 | 首屏总流量（KB） |
| 列表滑动 | 每屏增量流量 |
| 后台待机 | 静置 30 分钟后台流量是否为 0 |
| 图片加载 | 是否做了压缩、是否启用 gzip |

### 4.4 内存泄漏检测

**常见泄漏场景：**

| 场景 | 表现 |
|------|------|
| Activity/ViewController 被引用 | 退出页面后内存不降 |
| 未取消的网络回调 | 持有 Context/block 循环引用 |
| 未关闭的资源（Cursor、Stream） | FD 泄漏 |
| 静态集合持有对象引用 | 内存持续增长 |

**检测方法：**

- **Android：** 反复进出目标页面 5-10 次，观察 `dumpsys meminfo` 中 Java Heap 是否持续增长
- **iOS：** Instruments → Allocations → Mark Heap → 操作 → 再次 Mark Heap → 对比增长

---

## 五、崩溃分析

### 5.1 Android 崩溃日志获取

```bash
adb logcat -d > device_log.txt                    # 导出完整日志
adb logcat *:E | grep -A 20 "FATAL\|ANR\|CRASH"  # 过滤崩溃
adb pull /data/anr/traces.txt ./anr_traces.txt    # ANR 日志
```

**崩溃日志关键信息：**

```
FATAL EXCEPTION: main
Process: com.example.app, PID: 12345
java.lang.NullPointerException: ...
    at com.example.app.ui.ProfileFragment.onViewCreated(ProfileFragment.java:85)
```

需关注：异常类型、崩溃位置（类名:行号）、调用栈。

### 5.2 iOS 崩溃日志

```
Exception Type: EXC_BAD_ACCESS (SIGSEGV)
Thread 0 Crashed:
0   libobjc.A.dylib   objc_msgSend + 16
1   ExampleApp         -[ProfileViewController viewDidLoad] (ProfileViewController.m:42)
```

符号化命令：

```bash
xcrun atos -o ExampleApp.app.dSYM/Contents/Resources/DWARF/ExampleApp -l 0x00001234 -arch arm64
```

### 5.3 常见崩溃类型

| 崩溃类型 | Android | iOS | 触发场景 |
|----------|---------|-----|----------|
| **空指针** | NullPointerException | EXC_BAD_ACCESS | 对象未初始化或已释放 |
| **OOM** | OutOfMemoryError | Jetsam（被系统杀） | 图片过多、内存泄漏 |
| **ANR** | Application Not Responding | watchdog timeout | 主线程阻塞超 5s |
| **数组越界** | IndexOutOfBoundsException | NSRangeException | 数据与 UI 不同步 |
| **类型转换** | ClassCastException | unrecognized selector | 接口返回类型变化 |

### 5.4 崩溃收集平台

| 平台 | 支持平台 | 特点 |
|------|----------|------|
| **Bugly（腾讯）** | Android / iOS | 国内主流、免费 |
| **Sentry** | 全平台 | 开源、性能监控 |
| **Firebase Crashlytics** | Android / iOS | Google 官方、免费 |
| **Instabug** | Android / iOS | 用户反馈 + 崩溃 |

**崩溃率标准：** 优秀 < 0.1%，可接受 0.1%-0.5%，需关注 0.5%-1%，严重 > 1%。

---

## 六、安装/升级/卸载测试

### 6.1 全新安装测试点

```
□ 正常安装完成，图标显示正确
□ 首次启动引导页/权限弹窗正常展示
□ 存储空间不足时的安装提示
□ 安装过程中断后能否重新安装
□ 不同安装来源（应用商店、官网、ADB install）
□ 安装后权限默认状态是否正确
```

### 6.2 覆盖升级测试点

```
□ 从上一版本覆盖升级，用户数据保留
□ 从旧版本跨多版本升级，数据兼容
□ 升级后数据库迁移正常
□ 升级后登录状态保持
□ 升级后新功能可正常使用
□ 存储空间不足时的升级提示
```

**跨版本升级路径：** v1.0 → v2.0（直接）、v1.0 → v1.5 → v2.0（逐级）、v1.0 → v2.0（跳版本，最容易出问题）

### 6.3 卸载后重装测试点

```
□ 正常卸载完成，图标移除
□ 卸载后用户数据是否清除
□ 重装后是否为全新状态
□ iOS 注意：Keychain 数据可能残留！
```

### 6.4 应用商店审核注意事项

| 平台 | 关键审核点 |
|------|-----------|
| **App Store** | 隐私政策、权限说明、IPv6 兼容、热更新限制 |
| **Google Play** | 64 位支持、目标 API Level、数据安全声明 |
| **国内商店** | 软著证书、备案号、隐私合规 |

---

## 七、推送通知测试

### 7.1 推送到达率测试

```
□ App 在前台时收到推送
□ App 在后台时收到推送
□ App 被杀进程后收到推送
□ 设备重启后收到推送（App 未打开）
□ 网络断开后恢复，延迟推送是否补发
```

**到达率 = 成功展示通知数 / 发送推送数 × 100%，目标 > 95%**

### 7.2 推送点击跳转测试

```
□ 点击通知打开 App 并跳转到正确页面
□ 参数正确传递（订单ID、活动ID）
□ 冷启动后正确跳转
□ 通知已过期时的处理
```

### 7.3 免打扰/权限关闭场景

```
□ 用户关闭通知权限后不弹出通知
□ 开启免打扰模式不弹出通知
□ 应用内关闭推送开关后不收到推送
□ 权限关闭后重新开启恢复正常
```

### 7.4 多平台推送差异

| 平台 | 推送服务 | 特点 |
|------|----------|------|
| **iOS** | APNs | 统一通道、延迟低 |
| **Android 原生** | FCM | 需 Google 服务、海外为主 |
| **华为** | HMS Push | 后台保活能力强 |
| **小米** | MiPush | 可达性高 |
| **OPPO/vivo** | 各自推送 | 设备专属 |

**国内常见策略：** 有厂商推送的用厂商通道，其余回退 FCM 或自建通道。

---

## 八、手势与交互测试

### 8.1 手势测试要点

| 手势 | 测试点 | 常见问题 |
|------|--------|----------|
| 单击 | 按钮响应 | 点击区域过小、误触 |
| 双击 | 放大、点赞 | 与单击冲突 |
| 长按 | 弹出菜单 | 与系统长按冲突 |
| 滑动 | 列表滚动 | 滑动方向冲突 |
| 下拉刷新 | 列表刷新 | 与系统下拉冲突 |
| 双指缩放 | 地图/图片 | 缩放不流畅 |

### 8.2 横竖屏切换测试

```
□ App 支持自动旋转时，布局正确
□ App 锁定竖屏时，旋转不触发重绘
□ 切换过程中输入框内容不丢失
□ 视频播放页面横屏全屏正常
```

### 8.3 分屏模式

```
□ 进入分屏后布局自适应
□ 分屏状态下功能正常
□ 拖动分割线，布局动态调整
□ 小窗模式（画中画）表现
```

### 8.4 键盘弹出/收起

```
□ 输入框不被键盘遮挡
□ 键盘收起后页面恢复原位
□ 第三方键盘兼容
□ 安全键盘（密码输入）兼容
```

---

## 九、系统权限测试

### 9.1 权限测试矩阵

每项权限都需要测试三种状态：

| 测试状态 | 操作 | 预期表现 |
|----------|------|----------|
| **授予** | 用户允许权限 | 功能正常 |
| **拒绝** | 用户拒绝权限 | 功能降级，有提示，不崩溃 |
| **撤销** | 事后在设置中关闭 | 下次使用时重新请求或降级 |

### 9.2 运行时权限测试清单

| 权限 | 功能场景 | 拒绝后的降级方案 |
|------|----------|-----------------|
| **相机** | 扫码、拍照 | 使用相册选择 |
| **定位** | 地图、附近推荐 | 手动输入地址 |
| **存储/相册** | 保存图片、选文件 | 提示无权限 |
| **麦克风** | 语音输入、录音 | 使用文字输入 |
| **通知** | 推送消息 | 应用内提示开启 |

### 9.3 系统版本差异

| 版本 | 权限变更 |
|------|----------|
| Android 6.0 | 运行时权限 |
| Android 10 | 后台定位单独申请、分区存储 |
| Android 11 | 一次性权限、自动重置未使用权限 |
| Android 12 | 精确/模糊定位可选 |
| Android 13 | 通知权限需运行时申请 |
| iOS 14 | 精确/模糊定位可选、剪贴板提示 |
| iOS 16 | 照片部分访问、粘贴权限确认 |

---

## 十、移动端测试工具速查

### 10.1 adb 常用命令速查表

**设备管理：**

| 命令 | 说明 |
|------|------|
| `adb devices` | 列出已连接设备 |
| `adb install -r app.apk` | 覆盖安装 |
| `adb uninstall com.example.app` | 卸载应用 |
| `adb shell am start -n pkg/.Activity` | 启动 Activity |
| `adb shell am force-stop pkg` | 强制停止 |

**日志与调试：**

| 命令 | 说明 |
|------|------|
| `adb logcat` | 实时查看日志 |
| `adb logcat -c` | 清空日志 |
| `adb logcat -d > log.txt` | 导出日志 |
| `adb logcat *:E` | 只看 Error |
| `adb bugreport > bugreport.zip` | 完整 Bug 报告 |

**性能与操作：**

| 命令 | 说明 |
|------|------|
| `adb shell dumpsys meminfo pkg` | 内存详情 |
| `adb shell dumpsys cpuinfo` | CPU 使用 |
| `adb shell dumpsys battery` | 电池状态 |
| `adb shell screencap /sdcard/s.png` | 截图 |
| `adb shell screenrecord /sdcard/v.mp4` | 录屏 |
| `adb shell input tap 500 800` | 模拟点击 |
| `adb shell input swipe 500 1500 500 500` | 模拟滑动 |

### 10.2 Xcode Instruments 概述

| 工具 | 用途 |
|------|------|
| Time Profiler | CPU 分析、卡顿排查 |
| Allocations | 内存分配分析 |
| Leaks | 内存泄漏检测 |
| Zombies | 僵尸对象排查 |
| Network | 网络流量分析 |
| Energy Log | 电量分析 |

启动：Xcode ⌘I → 选择模板 → 录制 → 分析

### 10.3 Appium 特定配置

**Android：**

```json
{
  "platformName": "Android",
  "automationName": "UiAutomator2",
  "appPackage": "com.example.app",
  "appActivity": ".MainActivity",
  "noReset": true,
  "newCommandTimeout": 300
}
```

**iOS：**

```json
{
  "platformName": "iOS",
  "automationName": "XCUITest",
  "bundleId": "com.example.app",
  "noReset": true,
  "newCommandTimeout": 300
}
```

**常见问题：** Android 安装超时增大 `installTimeout`；iOS WebDriverAgent 安装失败检查证书；元素定位优先用 `accessibility-id`；混合应用需切换 Context。

---

## 附录：移动端测试检查清单

```
安装/升级/卸载   □ 全新安装  □ 覆盖升级  □ 卸载重装  □ 跨版本升级
功能完整性       □ 核心功能  □ 边界场景  □ 异常处理
兼容性           □ OS版本    □ 机型覆盖  □ 分辨率    □ 厂商ROM
弱网/断网        □ 2G/3G     □ 网络切换  □ 断网恢复
性能             □ 启动时间  □ 内存CPU   □ 电量流量
稳定性           □ 崩溃率    □ ANR率     □ Monkey
权限             □ 授予      □ 拒绝      □ 撤销
推送             □ 到达率    □ 点击跳转  □ 权限关闭
手势交互         □ 滑动点击  □ 横竖屏    □ 分屏键盘
UI适配           □ 刘海挖孔  □ 折叠屏    □ 深色模式  □ 大字体
```

---

> **总结：** 移动端专项测试的核心是覆盖"真实场景下的不可控因素"。弱网、兼容性、权限、崩溃是最高优先级。建议结合自动化框架（Appium / UIAutomator / XCUITest）将重复性测试自动化，把精力集中在探索性测试和新场景覆盖上。

---

## 动手任务：给一批移动端问题现象分类、设计复现方案并判定优先级

> 这是本教程的收尾练习。请**独立完成**，不要先看参考答案。目标不是"把问题转发给开发"，而是**用设备与网络矩阵把问题复现出来，并用可验证的证据定级**。

### 任务背景

App 新版本灰度发布后，客服、测试群、崩溃平台同时涌来一批零散的反馈，描述五花八门："我这台手机上打不开""在地铁里点支付没反应""刚打开就退了""死活不给定位"。

这些反馈目前只有**一句话描述**，没有设备型号、没有系统版本、没有网络环境、没有日志。你要做的是：**把它们分类、为每类设计可复现的方案（含设备与网络矩阵）、判定优先级**，输出一份可以让开发照着改、让测试照着复现的问题清单。

### 任务准备

| 项目 | 内容 |
|------|------|
| 被测 App | `com.example.shop` v3.8.0（灰度 20%），灰度前版本 v3.7.2 |
| 测试机 | 见下方"可用设备清单" |
| 弱网工具 | Charles / Fiddler（`Throttle` 预设）或手机自带网络调试 |
| 日志工具 | Android：`adb logcat`；iOS：Xcode Devices → View Device Logs |
| 崩溃平台 | Bugly（Android / iOS 均有上报） |
| 采集周期 | 灰度发布后第 1 天（24 小时） |

**可用设备清单（复现时只能从这里选）：**

| 编号 | 机型 | 系统 | 屏幕 | 备注 |
|------|------|------|------|------|
| D1 | 小米 14 | Android 15 | 6.36" 挖孔屏 | 高配，日常主测机 |
| D2 | 华为 Mate 60 | HarmonyOS 4.2 | 6.69" 挖孔屏 | 无 GMS |
| D3 | 三星 A54 | Android 14 | 6.4" 直屏 | 中端 |
| D4 | OPPO A17 | Android 12 | 6.56" 水滴屏 | 低端，2GB 可用内存 |
| D5 | iPhone 15 Pro Max | iOS 17.5 | 6.7" 灵动岛 | 高配 |
| D6 | iPhone SE（第 2 代） | iOS 16.7 | 4.7" 小屏 | 小屏代表 |
| D7 | vivo Y35 | Android 13 | 6.51" 直屏 | 中低端 |
| D8 | 小米 MIX Fold 3 | Android 14 | 展开 8.03" | 折叠屏代表 |

**一批用户反馈（原始描述，未经处理）：**

| 编号 | 反馈原文 | 反馈量 |
|------|----------|--------|
| R1 | "新版本在地铁里点支付，转圈半天最后又回到订单页" | 46 条 |
| R2 | "打开 App 3 秒内必闪退，7.0 的老手机" | 31 条 |
| R3 | "首页商品图片压扁了，右边的字被切掉了" | 22 条 |
| R4 | "怎么点都不给定位，附近门店一直转圈" | 63 条 |
| R5 | "扫一扫一直黑屏，重启也没用" | 18 条 |
| R6 | "用了一段时间手机特别烫，掉电飞快" | 27 条 |
| R7 | "折叠屏展开之后页面还是竖着的一条，商品格子全歪了" | 9 条 |
| R8 | "订单列表滑动一卡一卡的，像幻灯片" | 34 条 |
| R9 | "点了提交订单，订单出现了两条，钱扣了两次" | 5 条 |
| R10 | "升级之后之前保存的收货地址全没了" | 12 条 |

**崩溃平台 24 小时汇总（可用于交叉验证）：**

| 指标 | 数值 |
|------|------|
| 整体崩溃率 | 0.38%（v3.7.2 为 0.09%） |
| Top 1 崩溃 | `java.lang.OutOfMemoryError`（`ProductImageLoader`），占 41% |
| Top 2 崩溃 | `java.lang.NullPointerException`（`LocationFragment.onViewCreated`），占 23% |
| Top 3 崩溃 | `android.app.RemoteServiceException`（推送 Service），占 8% |
| ANR 率 | 0.21%（v3.7.2 为 0.05%） |
| Top 1 机型占比 | 低端机（≤ 3GB 可用内存）占崩溃量的 **67%** |
| Top 1 系统占比 | Android 12 及以下占崩溃量的 **58%** |

### 任务要求

请依次完成，并**保留每条命令与日志**：

1. **分类并对齐现象与根因**：把 R1-R10 归入分类（至少覆盖：**弱网与超时 / 兼容性（UI 适配、系统版本、折叠屏）/ 崩溃与稳定性（OOM、ANR）/ 权限 / 数据与升级 / 性能**）。为每一条写出：分类、最可能的根因方向、**判定它属于该类的依据**（引用反馈原文或崩溃平台数据）。注意 R3 与 R7 同属兼容性但根因不同，要分开写。
2. **为高风险项设计复现方案**：从分类结果中挑出**优先级最高的 3 条**，逐条写出可执行的复现方案，必须包含：设备型号（从设备清单选）、系统版本、网络参数（带宽 / 延迟 / 丢包）、精确操作步骤、以及**要采集的证据**（日志命令或工具）。
3. **构建两张矩阵**：① 一张**设备 × 问题**的兼容性矩阵（标出每台设备必须验的项）；② 一张**网络条件矩阵**（每条链路配什么弱网参数、用来复现哪些问题）。矩阵要能直接拿去当执行清单，而不是概念图。
4. **判定优先级并给出依据**：用「影响面 × 严重度 × 复现难度」给 R1-R10 排出 P0-P3，并明确写出 **P0 的判定标准**（什么样的问题必须 P0）。最后给出可复用产出：一份**提缺陷模板**，要求缺陷必须包含"可复现的设备 + 网络 + 步骤 + 日志"四要素。

### 提交物

| 产出 | 要求 |
|------|------|
| 分类表 | 编号 / 分类 / 可能根因 / 判定依据，覆盖 R1-R10 |
| 复现方案 | 优先级最高的 3 条，每条含设备、系统、网络参数、步骤、证据采集方式 |
| 兼容性矩阵 | 设备 × 检查项，可直接执行的勾选清单 |
| 网络条件矩阵 | 场景 / 带宽 / 延迟 / 丢包 / 目标复现项 |
| 优先级排序 | R1-R10 的 P0-P3 及依据（影响面 × 严重度 × 复现难度） |
| 缺陷模板 | 含"设备 + 网络 + 步骤 + 日志"四要素 |

### 完成标准

- [ ] 能区分**弱网问题**（可恢复、超时 / 重复提交）与**兼容性问题**（特定设备 / 系统必现），不会把两者混为一谈
- [ ] 能正确使用 `adb logcat` 过滤崩溃与 ANR，并知道 ANR 日志在 `/data/anr/traces.txt`
- [ ] 能读懂崩溃平台指标：崩溃率、Top 崩溃类型、机型占比，并用它反推问题聚类
- [ ] 能区分 **OOM**（内存不足被系统杀）与 **ANR**（主线程阻塞超时）的症状与排查手段
- [ ] 能写出**带参数的**弱网配置（带宽 / 延迟 / 丢包），而不是只写"模拟弱网"
- [ ] 能设计可执行的兼容性矩阵（含折叠屏、小屏、低端机），并给出优先级依据
- [ ] 优先级判定有明确标准，不是拍脑袋

??? tip "参考答案与思路（先自己做完再看）"

    **第 1 题：分类与根因**

    | 编号 | 分类 | 可能根因 | 判定依据 |
    |------|------|----------|----------|
    | R1 | 弱网与超时 | 弱网下支付请求超时，前端无超时兜底，回退订单页 | "地铁里"= 移动网络抖动；"转圈半天"= 请求长时间未返回 |
    | R2 | 崩溃与稳定性（OOM） | 低端机内存不足，图片加载未做降采样 | 崩溃平台 Top1 为 `OutOfMemoryError`，低端机占 67% |
    | R3 | 兼容性（UI 适配） | 布局未按容器缩放，窄屏下图片被压扁、长文案溢出被截断 | "图片压扁、右边字被切"= 布局溢出，与系统版本无关 |
    | R4 | 权限 | 定位权限被拒绝或系统限制，未走降级方案 | "怎么点都不给定位"= 权限层被拦，不是网络问题 |
    | R5 | 权限 / 兼容性 | 相机权限被拒，或个别机型摄像头预览异常；未处理黑屏兜底 | "一直黑屏"= 预览无画面，先查权限再查机型 |
    | R6 | 性能 | 后台唤醒 / 轮询过频，CPU 持续占用导致耗电发热 | "用了一段时间"= 累计效应，非瞬时崩溃 |
    | R7 | 兼容性（折叠屏） | 展开后未监听配置变化，页面未重绘 | "展开后仍是竖着一条"= 布局未响应屏幕尺寸变更 |
    | R8 | 性能（ANR 风险） | 列表滑动时主线程做 IO / 图片解码，导致掉帧；持续阻塞会转 ANR | "一卡一卡"= 掉帧；ANR 率从 0.05% 升至 0.21% |
    | R9 | 弱网与数据一致性 | 弱网下重复提交，后端幂等缺失 | 抓包可见重复请求；"订单出现两条"且重复扣款 |
    | R10 | 数据与升级 | 覆盖升级时本地存储 / 数据库迁移失败 | "升级之后……全没了"= 迁移逻辑缺陷，与网络无关 |

    两个易混点的区分：

    ```text
    R3（UI 溢出）vs R7（折叠屏未重绘）——同属兼容性，但根因不同：
      R3 是"静态布局问题"：在所有窄屏上都会溢出（D6 iPhone SE、D4 OPPO A17 可复现）
      R7 是"动态配置变更问题"：只在展开 / 折叠瞬间触发（只有 D8 可复现）
      → 复现设备不同、修复方向也不同（R3 改布局约束，R7 加 Configuration Change 监听 + 重绘）

    R2（OOM）vs R8（ANR）——同属稳定性，但症状与排查手段完全不同：
      OOM：进程被系统杀死 → 用户感知为"闪退"，抓 logcat 看 OutOfMemoryError，
           用 dumpsys meminfo 看 Java Heap / Graphics 是否持续增长
      ANR：进程还在但无响应 → 弹"应用无响应"或界面卡死，日志在 /data/anr/traces.txt，
           看主线程调用栈是否阻塞超过 5 秒
    ```

    **第 2 题：高风险项的复现方案**

    选 **R9（弱网重复下单，资损）、R2（低端机闪退）、R4（权限定位）** 三条。

    ```text
    复现方案 1 — R9 弱网下重复提交（严重度最高：资损）

    设备：D3 三星 A54 / Android 14（先在正常网络下确认不复现，作为对照）
    网络：Charles → Throttle 自定义
          带宽 50 kbps / 延迟 1000ms / 丢包 5%（模拟地铁弱网）
          另开 Breakpoints，把 /api/order/create 的响应延迟 8 秒后再放行
    步骤：
      1. 清空购物车，加入 1 件商品，进入订单确认页
      2. 打开弱网配置（50 kbps / 1000ms / 5% 丢包）
      3. 连续快速点击「提交订单」3 次（间隔 < 1 秒，模拟用户着急）
      4. 等待响应恢复，进入"我的订单"查看
    预期：仅生成 1 条订单，扣款 1 次
    实际（预期可复现）：生成 3 条订单 —— 前端未禁用按钮 + 后端未做幂等
    证据采集：
      - 抓包：导出 3 条 POST /api/order/create 的完整报文，
              比对请求体与请求头是否携带同一个幂等标识（requestId）
      - 日志：adb logcat -d > order_log.txt，再检索 orderId 出现次数
      - 数据：SELECT count(*) FROM orders WHERE user_id = <uid> AND sku_id = 90001
    ```

    ```text
    复现方案 2 — R2 低端机闪退（OOM）

    设备：D4 OPPO A17 / Android 12（2GB 可用内存；对照机：D1 小米 14 / Android 15）
    网络：不限（加载本地缓存同样能复现）
    步骤：
      1. adb shell dumpsys meminfo com.example.shop     # 记录基线 PSS
      2. 打开首页，连续进入 / 退出「商品详情」20 次（每次加载大图）
      3. 进入「我的」→ 上传头像（再占一份大图内存）
      4. 反复进出首页 → 我的 → 首页，直到崩溃
    预期：内存回收正常，进程稳定存活
    实际（预期可复现）：3 秒内闪退，logcat 报 OutOfMemoryError
    证据采集：
      - adb logcat -d > device_log.txt
      - adb logcat *:E > error_log.txt    # 再检索 FATAL EXCEPTION / OutOfMemoryError
      - 崩溃前后各跑一次 adb shell dumpsys meminfo com.example.shop，
        对比 Java Heap 与 Graphics 是否持续增长
      - Bugly 崩溃堆栈：ProductImageLoader.decode 未做 inSampleSize 降采样
    ```

    ```text
    复现方案 3 — R4 定位权限被拒后的降级

    设备：D2 华为 Mate 60 / HarmonyOS 4.2（无 GMS，定位走系统服务）
    网络：4G 正常（先排除网络干扰，聚焦权限问题）
    步骤：
      1. 设置 → 应用管理 → 本 App → 权限 → 位置信息 → 选择"禁止"
      2. 冷启动 App，进入「附近门店」
      3. 观察页面表现与日志
      4. 再把权限改回"仅使用期间允许"，回到页面下拉刷新
    预期：① 拒绝权限时给出明确提示并降级为手动输入地址；② 重新授权后能正常获取定位
    实际（预期可复现）：一直转圈、无任何提示（兜底缺失）；重新授权后需杀进程才能恢复
    证据采集：
      - adb shell dumpsys package com.example.shop | findstr "ACCESS_FINE_LOCATION"
      - adb logcat | findstr "LocationFragment"
      - 崩溃平台 Top2：LocationFragment.onViewCreated 空指针（未判空定位结果）
    ```

    **第 3 题：两张矩阵**

    ① 设备 × 问题兼容性矩阵（✅ 必测 / ○ 抽测 / — 不适用）：

    | 设备 | 弱网 R1/R9 | 崩溃 R2/R8 | UI 适配 R3 | 折叠屏 R7 | 权限 R4/R5 | 升级 R10 |
    |------|-----------|-----------|-----------|-----------|-----------|----------|
    | D1 小米 14 / A15 | ✅ | ○ | ✅ | — | ✅ | ✅ |
    | D2 华为 Mate 60 / 鸿蒙 4.2 | ✅ | ○ | ✅ | — | ✅ | ✅ |
    | D3 三星 A54 / A14 | ✅ | ○ | ○ | — | ○ | ✅ |
    | D4 OPPO A17 / A12（低端） | ✅ | ✅ 必测 | ✅ 窄屏 | — | ✅ | ✅ |
    | D5 iPhone 15 Pro Max / iOS 17 | ✅ | ○ | ✅ 灵动岛 | — | ✅ | ✅ |
    | D6 iPhone SE2 / iOS 16.7（小屏） | ○ | ✅ | ✅ 必测小屏 | — | ○ | ✅ |
    | D7 vivo Y35 / A13 | ○ | ✅ | ○ | — | ○ | ○ |
    | D8 小米 MIX Fold 3 / A14 | ○ | ○ | ✅ | ✅ 必测 | ○ | ○ |

    ② 网络条件矩阵：

    | 场景 | 带宽 | 延迟 | 丢包 | 目标复现项 |
    |------|------|------|------|-----------|
    | 弱 3G（地铁） | 200-500 kbps | 150-300ms | 1-2% | R1 支付超时 |
    | 极弱网 + 长延迟 | 50 kbps | 1000ms | 5% | R9 重复提交 |
    | 高延迟（无丢包） | 不限 | 500ms | 0% | 区分"高延迟"与"丢包"两类问题 |
    | 断网 | 0 | — | 100% | 离线兜底、断网下的提示与重试 |
    | 断网恢复 | 0 → 恢复 | — | 100% → 0% | 网络状态监听、恢复后页面能否继续 |
    | 弱 Wi-Fi | 5 Mbps | 100ms | 0.5% | 图片加载降级 / 白屏 |

    用法：**弱网相关的问题（R1 / R9）必须同时满足"网络条件"和"设备"两栏**——只换网络不换设备，是常见"复现不出来"的原因。

    **第 4 题：优先级判定**

    | 优先级 | 判定标准 |
    |--------|----------|
    | **P0** | 资损 / 数据丢失 / 核心链路不可用 / 崩溃率显著劣化。判定标准：**影响资金或数据的正确性，或影响面超过 10% 用户，或造成不可逆损失** |
    | P1 | 核心功能在特定环境 / 机型不可用，但有替代路径或影响面低于 10% |
    | P2 | 体验明显受损（卡顿、样式错乱），功能仍可用 |
    | P3 | 轻微体验问题、长尾机型 |

    排序结果：

    | 编号 | 影响面 | 严重度 | 复现难度 | 优先级 |
    |------|--------|--------|----------|--------|
    | R9 重复下单 / 重复扣款 | 中（5 条反馈，但均涉及资损） | 极高（资损） | 中（需弱网 + 连点） | **P0** |
    | R2 低端机闪退 | 高（低端机占崩溃量 67%，崩溃率 0.38%） | 高（进程被杀） | 低（D4 必现） | **P0** |
    | R10 升级后地址丢失 | 中（12 条，且用户数据不可逆丢失） | 高（数据丢失） | 低（D1 覆盖升级必现） | **P0** |
    | R4 定位不给提示 | 高（63 条，反馈量最高） | 中（有替代路径但无兜底） | 低 | P1 |
    | R5 扫码黑屏 | 中（18 条，核心功能不可用） | 中 | 低 | P1 |
    | R1 弱网支付超时 | 中（46 条） | 中（无超时兜底） | 中 | P1 |
    | R8 列表卡顿（ANR 风险） | 高（34 条） | 中（ANR 率已上升） | 中（需低端机 + 长列表） | P1 |
    | R6 发热耗电 | 中（27 条） | 低（不阻断功能） | 高（需长时间观测） | P2 |
    | R3 图片压扁 / 文字截断 | 中（22 条） | 低 | 低 | P2 |
    | R7 折叠屏未重绘 | 低（9 条，长尾机型） | 低 | 低 | P3 |

    注意两点：① **P0 看"后果的不可逆性"，不看反馈条数**——R9 只有 5 条反馈但涉及资损，必须 P0；R4 有 63 条反馈但可绕过，只到 P1。② R2 之所以是 P0 而不只是 P1，是因为它**不是个别机型问题**：崩溃平台显示低端机占崩溃量 67%，属于**崩溃率劣化**（0.09% → 0.38%，已进入"需关注"档，逼近"严重"档）。

    缺陷模板（可复用）：

    ```text
    【标题】<模块>_<现象>_<设备/网络条件>
            例：订单_弱网下重复提交资损_A54&50kbps

    【环境】
      App 版本：v3.8.0（灰度 20%）
      设备：三星 A54 / Android 14 / 可用内存 3.2GB
      网络：Charles Throttle 50 kbps / 延迟 1000ms / 丢包 5%
      前置条件：已登录 testuser，购物车中有 1 件商品

    【复现步骤】
      1. ……（每步都要具体到可执行，含等待时长 / 点击间隔）
      2. ……

    【实际结果】……
    【预期结果】……

    【证据】（缺一不可）
      抓包文件（含请求 / 响应完整报文）
      日志片段（adb logcat 过滤结果 / 崩溃堆栈，含时间戳）
      数据核对（DB 查询语句 + 结果）
      复现录屏（涉及交互时序时必须提供）

    【发生概率】5/5 次
    【优先级建议】P0，依据：涉及资损 + 数据重复写入
    ```
### 推荐下一步

根据你的学习进度，选择下一步：

1. **如果你想抓 App 的包**：学习 [Charles 抓包](../工具操作/Charles抓包教程-软件测试版.md)，移动端抓包配置最完整
2. **如果你想做 App 自动化**：学习 [Appium App 自动化](../自动化测试/Appium-App自动化教程-软件测试版.md)，把重复场景自动化
3. **如果你想准备面试**：学习 [移动端测试面试题](../面试专题/移动端测试面试题.md)，覆盖高频考点

### 阶段测验

完成教程后，建议做 [移动端专项测试测验](移动端专项测试测验.md) 检验学习效果。

### 通关检查

完成本阶段后，使用 [第3阶段-专项测试通关](../学习中心/第3阶段-专项测试通关.md) 检查是否可以进入下一阶段。
